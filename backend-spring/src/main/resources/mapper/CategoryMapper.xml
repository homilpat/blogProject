<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.knowledge.blog.mapper.CategoryMapper">

    <select id="findAll" resultType="com.knowledge.blog.model.Category">
        SELECT id, code, name, section, description, display_order, created_at
        FROM categories
        ORDER BY display_order ASC, id ASC
    </select>

    <select id="findById" resultType="com.knowledge.blog.model.Category">
        SELECT id, code, name, section, description, display_order, created_at
        FROM categories
        WHERE id = #{id}
    </select>

    <select id="findByCode" resultType="com.knowledge.blog.model.Category">
        SELECT id, code, name, section, description, display_order, created_at
        FROM categories
        WHERE code = #{code}
    </select>

    <insert id="insert" parameterType="com.knowledge.blog.model.Category" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO categories (code, name, section, description, display_order)
        SELECT #{code}, #{name}, #{section}, #{description}, COALESCE(MAX(display_order), 0) + 1
        FROM categories
    </insert>

</mapper>
