# 제조지식창고 블로그 기능 명세

## 1. 기술 구성

| 영역 | 기술 |
|---|---|
| 블로그 웹 | TypeScript, React, Next.js |
| 화면 스타일 | CSS, Tailwind CSS |
| 본문 편집기 | TipTap |
| 메인 백엔드 | Java 17, Spring Boot, MyBatis |
| AI/RAG 서버 | Python, FastAPI |
| 관계형 데이터베이스 | MySQL |
| 벡터 데이터베이스 | Qdrant |
| 실행 환경 | Docker Compose |

RAG 컨테이너는 Docker Compose의 GPU 전달 설정을 통해 NVIDIA RTX A4000을 사용합니다. BGE-M3 임베딩 계산은 CUDA 가속을 사용하며 LM Studio는 호스트 환경에서 별도로 GPU를 사용할 수 있습니다.

## 2. 사용자와 인증

- 사용자 회원가입 및 로그인
- JWT Bearer 토큰 기반 API 인증
- 비로그인, 일반 사용자, 관리자 권한 분리
- 일반 사용자와 관리자는 게시글 및 이미지를 등록할 수 있음
- 게시글 수정과 삭제는 관리자 권한 필요
- 카테고리 관리는 관리자 권한 필요
- 허용된 프론트엔드 주소만 접근하도록 CORS 설정

## 3. 게시글 기능

- 게시글 목록 조회
- Dashboard 게시물 카드의 제목·요약·여백을 포함한 전체 영역 클릭으로 원문 상세 이동
- 카테고리 및 섹션별 게시글 필터링
- 게시글 상세 조회
- 게시글 작성
- 게시글 수정
- 게시글 삭제
- 제목, 요약, 카테고리, 본문 및 태그 저장
- 조회 수 기록
- 게시글 등록 및 수정 후 RAG 색인 연동
- 새 글 작성 화면에서는 선택 제목과 필수 본문만 입력
- 제목을 입력하면 사용자가 지정한 제목을 유지
- 제목을 비워두면 게시 시 AI가 제목을 자동 생성
- 요약과 실제 주제 카테고리는 게시 시 자동 생성
- 요약은 원문 범위에서 추출해 AI가 본문에 없는 기술 내용을 추가하지 못하도록 제한
- LLM은 공정, 장비, 트러블슈팅, 수율·계측, AI 기술 등 실제 주제 카테고리 중 하나로 게시글을 분류
- `오늘의 학습 내용`은 게시글 카테고리가 아닌 별도의 게시물별 학습 정리 화면
- 각 학습 카드에는 실제 카테고리, 요약, 핵심 내용, 학습 방향 체크리스트 및 원문 링크를 표시
- 체크리스트 완료 상태는 현재 브라우저에 게시물별로 저장
- 게시글 상세 화면에는 사용자가 작성한 원문을 그대로 유지
- 본문 자동 분석은 선택적인 제목 생성, 원문 기반 요약, 실제 주제 카테고리 분류에 사용
- LM Studio 연결 실패 시 제목·요약은 원문으로 생성하고 카테고리는 임베딩 유사도로 분류
- 자동 생성 결과는 게시글 수정 화면에서 교정 가능
- 관리자 수동 재색인

게시글 작성 API:

```http
POST /api/posts
Content-Type: application/json
Authorization: Bearer <JWT>
```

## 4. 본문 편집기

본문은 TipTap 기반 블록 편집기로 작성합니다.

- 일반 문단 작성
- 굵게 및 기울임
- 제목 블록
- 글머리표 목록
- 인용문
- 표 삽입 및 행·열 추가/삭제
- 표 열 너비 조절 및 표 전체 삭제
- 이미지 블록 삽입
- 작성 화면에서 이미지 드래그앤드롭
- 클립보드 이미지 붙여넣기
- 기존 이미지 블록을 문단 사이로 드래그하여 순서 변경
- 이미지 모서리에서 크기 조절
- 게시글 수정 화면에서도 동일한 편집 기능 제공
- 기존 마크다운 게시글 표시 호환
- 제목이 비어 있으면 등록 및 수정 차단
- 본문에 실제 글자나 이미지가 없으면 등록 및 수정 차단
- 프론트엔드와 백엔드 API에서 본문 유효성을 각각 검사

새 게시글은 HTML 기반의 제한된 TipTap 콘텐츠로 저장합니다. 게시글 상세 화면에서는 TipTap이 인식하는 콘텐츠 노드만 렌더링합니다.

## 5. 이미지 업로드

이미지는 게시글을 저장하기 전에 별도 API로 업로드됩니다.

```http
POST /api/uploads/images
Content-Type: multipart/form-data
Authorization: Bearer <JWT>
```

요청 파일 필드명:

```text
file
```

성공 응답:

```json
{
  "url": "http://localhost:8080/uploads/생성된-이미지-파일명.jpg"
}
```

업로드 정책:

- JPG, PNG, GIF 지원
- 파일당 최대 10MB
- 최대 가로 또는 세로 12,000픽셀
- 로그인한 일반 사용자와 관리자만 업로드 가능
- 원본 파일명 대신 임의 UUID 파일명 사용
- MIME 형식과 실제 이미지 데이터를 함께 검사
- 파일 저장 경로 정규화 및 허용된 파일명 검사
- Base64 이미지는 본문에 직접 저장하지 않음
- Docker의 `blog_uploads` 볼륨에 이미지 저장
- 공개 게시글에서 볼 수 있도록 `/uploads/**` 경로로 이미지 제공

## 6. 이미지 자동 삭제

- 게시글 삭제 시 해당 게시글에 포함된 관리 대상 이미지를 탐색
- 다른 게시글이 사용하지 않는 이미지 파일만 삭제
- 게시글 수정 시 이전 본문과 새 본문을 비교
- 수정된 본문에서 제거된 이미지만 삭제 후보로 지정
- 다른 게시글이 같은 이미지 파일을 참조하면 파일 보존
- DB 트랜잭션이 정상 커밋된 후 실제 파일 삭제
- 파일 삭제 실패 시 서버 로그에 경고 기록
- 허용된 UUID 이미지 파일명만 삭제할 수 있도록 제한

사용자가 이미지를 업로드한 뒤 게시글을 저장하지 않고 작성 화면을 취소한 경우에는 해당 이미지가 즉시 삭제되지 않습니다. 이런 미사용 이미지는 향후 예약 정리 작업을 추가해 처리할 수 있습니다.

## 7. AI 분류와 RAG

- 게시글 제목과 본문을 이용한 AI 카테고리 추천
- 게시글 등록 시 자동 RAG 색인 요청
- 게시글 수정 시 기존 벡터 제거 후 재색인
- 게시글 삭제 시 관련 RAG 벡터 제거
- 게시글 상세 화면에서 색인 상태 표시
- 사용자 질문에 관련 제조 지식 검색 및 답변
- RAG 답변 생성 후 원문 대조 LLM 검증 수행
- 명시적인 `[근거 N]` 인용이 없는 생성 문장은 최종 답변에서 제거
- 1차 근거 필터를 통과한 문장을 LLM이 자연스럽게 재구성한 후 최종 근거 필터를 한 번 더 수행
- MySQL 게시글과 Qdrant 벡터 데이터 연동
- HTML 태그를 제거한 일반 텍스트를 RAG 색인에 전달
- 소형 로컬 LLM이 관련성은 있으나 직접 뒷받침되지 않는 근거 번호를 붙일 가능성은 남아 있으므로 중요한 답변은 표시된 원문 출처 확인 필요
- 답변 안의 `[근거 N]` 버튼과 근거 카드 버튼을 누르면 해당 원문 게시물을 새 탭으로 열기
- 여러 질문과 답변을 하나의 대화 세션으로 유지하고 사용자가 `대화 기록하기`를 눌렀을 때만 저장
- `새 대화` 이후의 세션은 별도 기록으로 저장하며 같은 세션 재저장 시 기존 기록 업데이트
- 저장된 대화는 로그인 사용자별 비공개 `지식창고와의 대화` 화면에서 조회 및 삭제
- `지식창고와의 대화`는 주제 탭이나 상단 메뉴에 섞지 않고 로그인 사용자의 왼쪽 사이드바에서만 접근
- 저장된 AI 대화는 게시글 및 RAG 벡터 색인 대상에서 제외

### 지식창고와의 대화 저장 방식

- AI 검색 화면에서 `새 대화`를 누른 이후의 질문·답변·근거 목록을 하나의 대화 세션으로 관리
- `대화 기록하기`를 누를 때만 `saved_conversations` 테이블에 저장하며 자동 저장하지 않음
- 기록 제목은 첫 질문을 기준으로 자동 생성
- 기록한 대화에서 질문을 더 한 뒤 `대화 기록 업데이트`를 누르면 같은 대화 ID를 갱신
- `새 대화`를 누른 뒤 기록하면 새로운 대화 ID로 별도 저장
- 저장 데이터에는 질문, 검증된 최종 답변, 근거 게시물 ID·제목·청크·유사도·원문 URL 스냅샷을 포함
- 대화 목록 조회·수정·삭제 시 JWT의 사용자 ID를 조건으로 사용해 다른 사용자의 기록 접근 차단
- 대화 저장 서비스는 `PostService`와 RAG 색인 API를 호출하지 않으므로 Qdrant에 저장되지 않음
- 답변 안의 `[근거 N]` 버튼 또는 근거 카드의 제목 버튼을 누르면 `/posts/{id}` 원문을 새 탭으로 열기

대화 기록 API:

```http
GET    /api/conversations
POST   /api/conversations
PUT    /api/conversations/{id}
DELETE /api/conversations/{id}
Authorization: Bearer <JWT>
```

## 8. 보안 설계

현재 적용된 항목:

- JWT 기반 인증 및 역할별 API 권한 검사
- 업로드 파일 크기와 이미지 해상도 제한
- 허용된 이미지 형식 제한
- 실제 이미지 디코딩 검사
- UUID 파일명 사용
- 경로 조작 방지
- 업로드 디렉터리 외부 파일 삭제 차단
- DB 커밋 이후 이미지 파일 삭제
- 프론트엔드 CORS 허용 주소 제한
- 비밀정보를 `.env`로 분리
- 본문을 `dangerouslySetInnerHTML`로 직접 실행하지 않고 TipTap 스키마로 렌더링

외부 운영 배포 전에 추가 권장되는 항목:

- HTTPS 적용
- Spring Boot 서버 측 HTML 살균
- 업로드 및 로그인 요청 속도 제한
- 악성 파일 또는 바이러스 검사
- 이미지 저장소 백업
- 주기적인 미사용 이미지 정리
- 관리자 감사 로그
- JWT 비밀키 정기 교체
- 운영 환경의 보안 헤더와 리버스 프록시 설정
- 데이터베이스와 Docker 볼륨 자동 백업

## 9. 데이터 보존

| 데이터 | 저장 위치 | 일반 재시작 후 보존 |
|---|---|---|
| 사용자, 카테고리, 게시글 | MySQL `mysql_data` 볼륨 | 보존 |
| RAG 벡터 | Qdrant `qdrant_data` 볼륨 | 보존 |
| 게시글 이미지 | `blog_uploads` 볼륨 | 보존 |

`docker compose stop`, `docker compose restart`, `docker compose down`은 이름 있는 볼륨을 기본적으로 보존합니다.

`docker compose down -v`를 실행하면 위 데이터 볼륨이 삭제될 수 있으므로 운영 데이터가 있다면 사용하지 않습니다.

## 10. 현재 제한사항

- 작성 중 이미지를 업로드한 뒤 게시글을 저장하지 않고 취소하면 미사용 이미지가 남을 수 있음
- 서버 측 HTML 살균과 요청 속도 제한은 추가 보강 권장
- 이미지 CDN, 썸네일 자동 생성 및 이미지 포맷 최적화는 아직 제공하지 않음
- 운영 환경 백업과 복구는 별도 인프라 구성이 필요함
